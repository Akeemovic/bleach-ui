// Buttons
Natural Buttons like => button, [type="button"], [type="reset"], [type="submit"]
Inherit the default button bu default and the modifier classes can be called directly..

Other elements need the ".button" class or a colour modifier class to be styled as a button... Then, a shape modifier class can follow

btn-full gives it 100% width and block display

// Containers
Just containers


// Forms
Form input fields are styled automatically
Class form-group can be used to wrap label and input field - It separate them by adding 20px padding
Class form-square to turn the form field to a square shape
